# Grafana Cloud Configuration
# Create an API token in Grafana Cloud: Settings → API Keys → New API Key (Editor role)

GRAFANA_URL=https://YOUR-INSTANCE.grafana.net
GRAFANA_API_TOKEN=glc_YOUR_API_TOKEN_HERE

# Grafana Cloud Prometheus/Loki Configuration (for Grafana Agent)
# Find these in: Grafana Cloud → My Account → Details & API Keys
# Or: Grafana Cloud → Connections → Add new connection → Hosted Prometheus/Loki metrics

# Prometheus Push URL (e.g., https://prometheus-prod-01-eu-west-0.grafana.net/api/prom/push)
PROMETHEUS_URL=https://prometheus-XXXXX.grafana.net/api/prom/push

# Prometheus Instance ID (username) - find in Prometheus details
PROMETHEUS_INSTANCE_ID=123456

# Prometheus API Token (glc_...)
PROMETHEUS_API_TOKEN=glc_YOUR_PROMETHEUS_TOKEN_HERE

# Loki Push URL (e.g., https://logs-prod-006.grafana.net/loki/api/v1/push)
LOKI_URL=https://logs-XXXXX.grafana.net/loki/api/v1/push

# Loki Instance ID (username) - find in Loki details (may differ from Prometheus)
LOKI_INSTANCE_ID=654321

# Loki API Token (glc_...)
LOKI_API_TOKEN=glc_YOUR_LOKI_TOKEN_HERE
